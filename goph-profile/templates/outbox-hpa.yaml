{{- if .Values.app.deployment.outbox.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Chart.Name }}-outbox
  namespace: {{ .Chart.Name }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Chart.Name }}-outbox
  minReplicas: {{ .Values.app.deployment.outbox.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.app.deployment.outbox.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.app.deployment.outbox.autoscaling.targetCPUUtilizationPercentage }}
      - type: Resource
        resource:
          name: cpu
          target:
            type: Utilization
            averageUtilization: {{ .Values.app.deployment.outbox.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
{{- end }}
