apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-server
  namespace: {{ .Chart.Name }}
spec:
  type: {{ .Values.app.deployment.server.service.type}}
  selector:
    app: {{ .Chart.Name }}-server
  ports:
    - name: http
      port: 80
      targetPort: 8080